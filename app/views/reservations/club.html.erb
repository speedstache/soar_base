<div class="container mt-2 mb-5 pb-5">
	<div class="row">
		<div class="col justify-content-start">
			<h4>Availability for: <%= @res_show.strftime("%a, %b %d, %Y") %></h4>
		</div>
		<div class="col input-group justify-content-end">
			<%= form_tag(reservations_club_path, method: :get) do %>
				<%= submit_tag ("Check reservations"), {class: "btn btn-info btn-sm"} %>
				<%= select_tag(:search, options_from_collection_for_select(@avail_days, 'day', 'day'),{class: "form-select append"}) %>
			<% end %>
		</div>
		<div>
			<p>Check open slots by selecting date and clicking Check Reservations </p>
		</div>
		<div class="table-responsive">
			<%= render 'grid' %> 
			<p>Click on red (booked) dot to see reservation details</p> 
		</div>
	</div>
<div class="row">
	<div class="col justify-content-start">
		<h4>Reservation list for: <%= @res_show.strftime("%a, %b %d, %Y") %></h4>
	</div>
	<div class="row">
		<div class="col-sm-10 col-md-8 table-responsive">		
			<table class="table table-striped">
				<thead>
					<tr>
						<th>Name  </th>
						<th>Date  </th>
						<th>Time  </th>
						<th>Aircraft </th>
						<th>Instructor?</th>
					</tr>
				</thead>
					<tbody>
						<% @res_date.each do |reservation| %>
							<tr>
								<td><%= reservation.user.username %></td>
								<td><%= reservation.reservation_date.strftime("%m/%d/%y") %>  </td>
								<td><%= reservation.reservation_time %></td>
								<td><%= reservation.aircraft.short_name %>  </td>
								<td><%= reservation.instructor_flag.humanize %>  </td>
							</tr>
						<% end %>
					</tbody>
				</table>
			</div>
		</div>		
	</div>
</div>
		