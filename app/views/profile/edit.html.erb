<div class="container my-2 mx-2">
	<div class="row justify-content-start" >
    <h4>Update Personal Info for <%= current_user.username %> </h4>
      <div class="col-sm-10 col-md-6">
      <%= render 'shared/errors', obj: current_user %>
 
      <%= render 'form' %>

    </div>
    <div class="mb-5 pb-5">
      <%= link_to "Back to My profile", profile_index_path,  class: "btn btn-secondary btn-sm" %>
    </div>
  </div>
</div> 
