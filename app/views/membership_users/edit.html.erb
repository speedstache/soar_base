<div class="container mx-2 my-2">
	<div class="row text-center justify-content-center" >
    <div class="col-sm-10 col-md-6">
    <h4>Edit or renew user membership </h4>
      <p class="text-muted">If 'Renew' was selected, renewal period (usually 365 days) is added automatically. 
      If 'Edit' was selected, renewal date, membership type, and active status can be edited 
      before submitting.</p>

      <%= render 'shared/errors', obj: @membership_user %>
      <%= render 'form' %>
    </div>
  </div>
</div>
