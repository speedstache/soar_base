<%= form_with(model: membership_user) do |form| %>
  <% if membership_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(membership_user.errors.count, "error") %> prohibited this membership_user from being saved:</h2>

      <ul>
        <% membership_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
